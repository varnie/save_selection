[project]
name = "vocab_app"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "requests==2.34.2",
    "sqlalchemy==2.0.49",
    "deep-translator==1.11.4",
]

[tool.ruff]
line-length = 120
target-version = "py39"

[tool.ruff.lint]
preview = true
select = [
    "E",     # pycodestyle errors
    "W",     # pycodestyle warnings
    "F",     # pyflakes
    "I",     # isort
    "N",     # pep8-naming
    "UP",    # pyupgrade
    "B",     # flake8-bugbear
    "C4",    # flake8-comprehensions
    "SIM",   # flake8-simplify
    "S",     # flake8-bandit (security issues)
    "RUF",   # Ruff-specific rules
]
[tool.ruff.lint.per-file-ignores]
"src/infrastructure/tray/tray_macos.py" = ["N802"]  # ObjC protocol
"src/vocab_gui.py" = ["E402"]  # gi.require_version must precede GTK import
"src/windows/*.py" = ["E402"]  # gi.require_version must precede GTK import
"src/infrastructure/tray/tray_linux.py" = ["E402"]  # gi.require_version must precede GTK import
"src/infrastructure/clipboard.py" = ["S404"]  # subprocess used safely
"src/infrastructure/notifications.py" = ["S404"]  # subprocess used safely
"src/infrastructure/tray/__init__.py" = ["RUF067"]  # conditional imports needed
"src/repositories/__init__.py" = ["RUF067"]  # factory registration needed
"src/tests/**/*.py" = [
    "S101",   # assert — standard in pytest
    "S108",   # /tmp paths — all mocked in tests
    "SIM117", # nested with — stylistic, tests prefer readability
    "RUF001", # Cyrillic in test data — intentional
    "RUF069", # float equality 2.5 — exact init value, not computed
]
"src/application/__init__.py" = ["RUF067"]  # non-trivial code, not just re-exports
"src/windows/__init__.py" = ["RUF067"]  # shared GTK helpers by design
